#include <stdio.h>
#include <math.h>
#include <complex.h>

struct polar {
  double mag; // magnitude component
  double ang; // angle component
};

struct rect {
  double re; // real component 
  double im; // imaginary component 
};

/* double polar_to_rect(double rect_num) { */
  

/*   return */ 
/* } */

int main() {

  return 0;
}
